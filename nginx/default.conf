server {
    listen 80;
    location /static/ {
        root /var/www/;
    }
    location /media/ {
        root /var/www/;
    }
    location / {
        proxy_path http://web:8000;
    }
    server_tokens off;
}